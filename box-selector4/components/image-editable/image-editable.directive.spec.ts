import { ImageEditableDirective } from './image-editable.directive';

describe('ImageEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageEditableDirective();
    expect(directive).toBeTruthy();
  });
});
