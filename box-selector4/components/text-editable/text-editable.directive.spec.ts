import { TextEditableDirective } from './text-editable.directive';

describe('TextEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditableDirective();
    expect(directive).toBeTruthy();
  });
});
